{
  "name": "microagent-copilot",
  "displayName": "microagent-copilot",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.93.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "micro-agent-chat",
          "title": "MicroAgent Chat",
          "icon": "resources/chat-icon.svg"
        }
      ]
    },
    "views": {
      "micro-agent-chat": [
        {
          "type": "webview",
          "id": "micro-agent-chat",
          "name": "MicroAgent Chat"
        }
      ]
    },
    "commands": [
      {
        "command": "copilot.MicroAgent",
        "title": "MicroAgent"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.7",
    "@types/node": "20.x",
    "@types/vscode": "^1.93.0",
    "@typescript-eslint/eslint-plugin": "^8.3.0",
    "@typescript-eslint/parser": "^8.3.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.9.1",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "dedent": "^1.5.3",
    "dotenv": "^16.4.5",
    "glob": "^11.0.0",
    "openai": "^4.47.1"
  }
}
